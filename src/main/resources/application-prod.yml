spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
  thymeleaf:
    cache: true

app:
  ecos:
    api-key: ${ECOS_API_KEY}
    base-url: https://ecos.bok.or.kr/api
  gemini:
    api-key: ${GEMINI_API_KEY}
    model: gemini-3-flash-preview
    image-model: imagen-4.0-fast-generate-001
    timeout-seconds: 30
    text-api-url: https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent
    image-api-url: https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:predict
  admin-key: ${ADMIN_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.example.kreconomonmon: INFO
  file:
    name: /var/log/kr-economy-mon/app.log
